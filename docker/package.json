{
  "name": "docker",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/index",
    "start": "./dist/index"
  },
  "dependencies": {
    "hono": "^4.12.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
